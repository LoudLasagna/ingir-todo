{
  "name": "in-gir-todo",
  "version": "1.0.0",
  "description": "Простое приложение для управления списком задач на vite + vue",
  "main": "index.js",
  "scripts": {
    "install": "cd front && npm i && cd ../back && npm i",
    "dev": "concurrently \"cd back && npm start\" \"cd front && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
